apiVersion: v1
kind: PersistentVolume
metadata:
    name: pv-mysql
    labels:
        type: local
spec:
    capacity:
        storage: 1Gi
    accessModes:
        - ReadWriteMany
    hostPath:
        path: /var/lib/mysql